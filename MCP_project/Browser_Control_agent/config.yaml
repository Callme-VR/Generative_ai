mcp_servers:
  stitch:
    transport:
      type: sse
      url: https://stitch.googleapis.com/mcp
  playwright:
    command: npx
    args:
      - "-y"
      - "@modelcontextprotocol/server-playwright"
